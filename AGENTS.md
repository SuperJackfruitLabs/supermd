# AGENTS.md

The project instructions live in [CLAUDE.md](./CLAUDE.md). Read that file before
working in this repository. It owns the architecture, commands, testing rules,
and generated documentation workflow; keep those instructions in one place.

For an editor behavior regression, use the repository-owned
[editor regression skill](.agents/skills/sjl-editor-regression/SKILL.md). It maps
the pure Rust and GPUI evidence needed for that defect. The skill is a candidate;
its presence does not establish native verification or cross-harness parity.
