name: CI

on:
  push:
    branches: [master]
  pull_request:

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true
      - name: Linux build deps
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libxkbcommon-dev libxkbcommon-x11-dev \
            libwayland-dev libx11-xcb-dev libxcb1-dev libfontconfig1-dev \
            libfreetype6-dev
      - name: Run tests
        run: cargo test
      - name: Build
        run: cargo build

  coverage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: llvm-tools-preview
      - uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true
      - name: Linux build deps
        run: |
          sudo apt-get update
          sudo apt-get install -y libxkbcommon-dev libxkbcommon-x11-dev \
            libwayland-dev libx11-xcb-dev libxcb1-dev libfontconfig1-dev \
            libfreetype6-dev
      - uses: taiki-e/install-action@cargo-llvm-cov
      - name: Measure coverage
        # Excluded: shell files still mostly out of unit-test reach
        # (window/element plumbing; workspace and main are next in line as
        # gpui-test-support coverage grows). editor/mod.rs and search_ui.rs
        # earned their way back into scope via TestAppContext tests.
        run: |
          cargo llvm-cov --json --summary-only \
            --ignore-filename-regex 'src/(view|input|reader|workspace|main)\.rs' \
            > coverage.json
          PCT=$(jq -r '.data[0].totals.lines.percent | . * 10 | round / 10' coverage.json)
          echo "PCT=$PCT" >> "$GITHUB_ENV"
          echo "### Test coverage: ${PCT}% (lines)" >> "$GITHUB_STEP_SUMMARY"
      - name: Update coverage badge
        if: github.event_name == 'push' && github.ref == 'refs/heads/master'
        uses: schneegans/dynamic-badges-action@v1.7.0
        with:
          auth: ${{ secrets.GIST_SECRET }}
          gistID: 9fb5226b83eda4ae8cb0568e7bc7755f
          filename: supermd-coverage.json
          label: coverage
          message: ${{ env.PCT }}%
          valColorRange: ${{ env.PCT }}
          minColorRange: 40
          maxColorRange: 90
