# ZenUML SVG assets

These assets are vendored from `@zenuml/core` 3.50.1 (`38404ccc`) and are consumed by the
headless ZenUML SVG renderer. They retain the upstream MIT license in this directory.

The file names are normalized for stable Rust `include_str!` paths. Their SVG contents are
otherwise unchanged from the upstream release.
