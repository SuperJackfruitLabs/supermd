[package]
name = "calc"
version = "0.1.0"
edition = "2021"

[workspace]

[lib]
# rlib so the evaluator is unit-testable on the host target.
crate-type = ["cdylib", "rlib"]

[dependencies]
wit-bindgen = "0.60"
